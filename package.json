{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && jscs src && pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "jscs": "^2.11.0",
    "jshint": "^2.9.5",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.5.0"
  }
}
