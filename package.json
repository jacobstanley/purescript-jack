{
  "private": true,
  "scripts": {
    "postinstall": "../../../scripts/install-purescript",
    "test": "npm run ptest",
    "pbuild": "spago build",
    "ptest": "spago test",
    "format": "../scripts/format"
  },
  "dependencies": {},
  "devDependencies": {
    "spago": "^0.20.3",
    "eslint": "^8.16.0",
    "prettier": "^2.6.0",
    "purs-tidy": "^0.7.2"
  }
}
